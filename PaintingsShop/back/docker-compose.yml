services:
  db:
    image: postgres:16
    container_name: shop_docker
    restart: always
    environment:
      POSTGRES_DB: shop_db
      POSTGRES_USER: root
      POSTGRES_PASSWORD: root
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: